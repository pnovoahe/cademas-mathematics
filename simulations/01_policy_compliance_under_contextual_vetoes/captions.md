# Captions (Section 6.1)

These captions match `first_round_v2/manuscriptR1V2.tex`.

## Figure: policy_violation_rate.pdf (`fig:policy-violation`)

Policy violation rate $V$ under contextual vetoes ($N=1000$, $K=100$, $1000$ Monte Carlo replications). Points are means; error bars and shaded bands are $95\%$ percentile confidence intervals. (\textbf{a}) Representative $\lambda\in\{0.50,0.75,0.90\}$. (\textbf{b}) Dense sweep over $\lambda\in[0,1]$.

## Table: table_policy_compliance.tex (`tab:policy-compliance`)

Monte Carlo mean, standard deviation, and $95\%$ percentile confidence interval of the policy violation rate $V$ at $\lambda\in\{0.50,0.75,0.90\}$ ($N=1000$, $K=100$, $1000$ replications).
