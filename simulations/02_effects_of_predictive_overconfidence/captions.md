# Captions (Experiment 02)

Generated from Monte Carlo results. Not manuscript-ready prose.

## Figure: overconfidence_v_utility (`fig:overconfidence-v-utility`)

Policy violation rate $V$ and predictive utility $\bar{R}$ (mean original $R$ of the selected Top-$K$) versus overconfidence level $\delta$ at $\lambda=0.75$ ($N=1000$, $K=100$, $q_{\mathrm{weak}}=0.25$, 1000 Monte Carlo replications). Points are means; shaded bands are $95\%$ percentile confidence intervals. (\textbf{a}) $V(\delta)$. (\textbf{b}) $\bar{R}(\delta)$.

## Figure: overconfidence_kendall_tau (`fig:overconfidence-kendall`)

Kendall $\tau$ between the unperturbed ranking ($\delta=0$) and the ranking under overconfidence $\delta$, at $\lambda=0.75$. Same Monte Carlo protocol as the two-panel figure.

## Table: table_predictive_overconfidence.tex (`tab:predictive-overconfidence`)

Monte Carlo mean, standard deviation, and $95\%$ percentile CI of $V$ and $\bar{R}$ at $\lambda=0.75$ for $\delta\in\{0.00, 0.10, 0.20, 0.30\}$.

## Table: table_overconfidence_kendall.tex (`tab:overconfidence-kendall`)

Kendall $\tau$ and Jaccard Top-$K$ at the same representative $\delta$ values and $\lambda=0.75$.
